使用 python3.12.4，pandas2.2.2，scikit-learn1.5.1

注意：原始数据（data/raw/12307_null_dic.csv）第一列为 id（不纳入训练），倒数第二列是用现行 sic 判断标准判断出来的 sic（不纳入训练），death 代表结局（不纳入训练），最后一列 dic 作为标签

缺失值处理：删去缺失比例大于 0.5 的特征（删除 0 个），删除缺失值个数大于 5 个的行（原有12307行，删除3347行，剩下8960行），然后用组内的列均值对缺失的连续性变量进行插补填充，缺失的分类变量则统一划分为＂0＂，即表示不存在该种情况。保存为 data/清洗后.csv

注意：标签不平衡（缺失值处理后，0:1=8531:428），后续考虑采用 f1 作为超参数优化目标

采用分层抽样策略，按8:2比例将数据集划分为训练集（7,168条）与测试集（1,792条），确保两集合的标签分布与原始数据一

对连续变量实施Z标准化统一量纲，从而促进不同变量的比较，保存为data/train_data_standardized.csv，和datatest_data_standardized.csv"

手动删除 id，death，sic 这三列（不纳入训练），把标签列（dic）移动到第一列


